<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <ImageButton
        android:id="@+id/exitPistasEntrenamiento"

        android:layout_width="65dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:layout_marginLeft="10dp"
        app:srcCompat="@drawable/abc_vector_test" />
    <TextView
        android:id="@+id/listaPistas"
        android:layout_below="@+id/exitPistasEntrenamiento"
        android:layout_width="match_parent"
        android:layout_height="51dp"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:text="Pistas del entrenamiento"
        android:textSize="30dp" />
    <ListView
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:id="@+id/listaPistasTraining"
        android:layout_below="@+id/listaPistas"
        />

</RelativeLayout>